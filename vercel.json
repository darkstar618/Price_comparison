{
  "version": 2,
  "builds": [
    { "src": "api/search.py", "use": "@vercel/python" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "api/search.py" },
    { "src": "/(.*)", "dest": "/public/$1" }
  ]
}
